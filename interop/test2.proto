// For testing Oneof, Map

syntax = "proto2";
// No package

message OneofMessage {
    oneof first {
        string name = 4;
        sint64 number = 9;
    }
    oneof second {
        string foo = 1;
        double bar = 2;
    }
}

message MapMessage {
    map<string, sint64> number_ints = 5;
}
